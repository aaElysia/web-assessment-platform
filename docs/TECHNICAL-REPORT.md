# 技术报告（交付物 E）

> 对应 *Agentic AI Web Assessment Challenge* 的 **Deliverable E**。
> 覆盖挑战赛要求的 11 个问题。真实用户试点（Deliverable D）已完成并独立分析，详见 `docs/PILOT-EVALUATION.md`；本文 §7 给出精炼结论并交叉引用。

---

## 1. 我们构建了什么

一个**已正式部署、可公开访问的 Web 交互式测评平台**，用于同时评估用户的两类特征：

- **人格**：采用成熟的**大五人格（Big Five）**框架；
- **态度/行为倾向**：自选主题为**对人工智能的采纳态度（AI Adoption Attitude）**。

平台端到端包含：知情同意 → 60 题交互式问卷（5 点 Likert）→ 自动评分 → 个性化交互式结果（图表 + 解读）→ 匿名数据入库 → 研究者管理后台（聚合统计、信度/相关分析、CSV 导出、带删除与排序的提交明细）。

- 公开 URL（部署于 Vercel）：`https://web-assessment-platform-q7km6nfsp-xin-yunpeng.vercel.app`
- 源码仓库（GitHub）：`https://github.com/aaElysia/web-assessment-platform`

---

## 2. 测量了哪些心理 / 人格构念

**人格（大五，Big Five）**——5 个维度，每维 8 题共 40 题：

| 维度 | 含义 |
|---|---|
| O 开放性（Openness） | 对经验、创意、抽象思考的开放程度 |
| C 尽责性（Conscientiousness） | 条理性、自律、目标导向 |
| E 外向性（Extraversion） | 社交能量、外向程度 |
| A 宜人性（Agreeableness） | 合作、信任、利他倾向 |
| N 神经质（Neuroticism） | 情绪不稳定性、负面情绪易感性 |

**态度（AI 采纳态度）**——5 个维度，每维 4 题共 20 题：

| 维度 | 含义 | 语义方向 |
|---|---|---|
| PU 感知有用性（Perceived Usefulness） | 相信 AI 有用 | 正向（越高越积极） |
| TR 信任（Trust） | 对 AI 的信任 | 正向 |
| WA 采纳意愿（Willingness to Adopt） | 使用/采纳 AI 的倾向 | 正向 |
| LA 学习态度（Learning Attitude） | 学习 AI 的意愿 | 正向 |
| CN 对 AI 的担忧（Concern） | 对 AI 的顾虑/担忧 | **反向**（越高表示担忧越多，并非越积极） |

> 全题库共 **60 题**，其中 **29 道为反向表述题**（已在题库中显式标记 `reverse: true`），用于抑制应答定势（acquiescence）与随意勾选。

---

## 3. 为什么选择这些构念

- **大五人格是心理测量学中最被广泛验证、最稳健的人格模型之一**，信效度证据充分，且题目成熟、易于公开获取与改编。对一个"展示如何正确做心理测量"的项目而言，使用成熟框架比自创模型更负责任、也更可信。
- **AI 采纳态度**契合"AI 辅助开发"这一任务背景，具有现实意义；其五维度（有用性/信任/意愿/学习/担忧）覆盖了技术接受模型（TAM）与相关文献的常见要素，既有正向维度也有一个反向维度（担忧），便于演示"反向题处理"与"语义方向澄清"。

---

## 4. 评分系统如何工作

评分引擎是**配置驱动、纯函数**实现（`src/lib/scoring/score.ts`），新增题目无需改动评分代码。

1. **反向重编码**：5 点制下，`recoded = (scale + 1) − value = 6 − value`，每个反向题仅做一次题目级翻转。
2. **维度分**：维度内有效题得分取均值，保留 4 位小数。
3. **缺失策略**：单维度内**缺失 > 1 题**则该维度判为 `incomplete`（分数 = `null`），不参与任何均值；缺失恰好 1 题则用已作答项均值填补（不臆造）。
4. **分带（band）**：仅做**相对量表中点 3.0** 的启发式判断（高于中点 / 接近中点 / 低于中点），**明确声明无常模**，避免把"相对中点"误读成"高/低人格"。
5. **AI 合成指数（探索性、等权）**：`mean(PU, TR, WA, LA, 6 − CN) / 5`，即五个域（CN 在域级翻正）的算术平均，范围 1–5，每域权重 0.2。任一域不可用则指数返回 `null`（不猜测、不部分合成）。该指数**明确降级为"补充性的整体倾向参考"**，不代表一个已被验证的单维构念。
6. **聚合分析指标**（管理后台）：Cronbach's α（含 bootstrap 置信区间）、Pearson r 相关矩阵、完成率、题项级反应分布、参与人数。

`docs/assessment-framework.md` 是该评分逻辑的**权威规格文档**。

---

## 5. 使用了哪些技术

- **前端 / 框架**：Next.js 14（App Router）、React、TypeScript、Tailwind CSS。
- **后端 / 数据**：Next.js Route Handlers（API 路由）、Prisma ORM、SQLite（本地开发）/ PostgreSQL on Neon（生产）、自研签名会话鉴权（HMAC-SHA256 Cookie）。
- **可视化**：SVG 图表（雷达图、条形图、分布图），无重型图表库依赖。
- **部署**：Vercel（前端 + 函数）+ Neon（托管 Postgres），`vercel-build` 构建链在生产时切换 Prisma provider。
- **验证**：Vitest（256 项测试）、tsx 脚本（E2E 契约测试、浏览器渲染冒烟、独立评分交叉验证、密钥审计）。
- **AI 辅助开发工具**：WorkBuddy（agentic AI 编程助手），详见交付物 C。

---

## 6. 如何使用 agentic AI

全程在 agentic AI 编程助手协助下开发，人类保留最终责任与验证权。关键实践：

- **先审查、后实现、再验证**：对评分系统做了一轮只读"心理测量审查"，对界面做了一轮只读"UX 审查"，确认问题清单后，才按优先级落地修改。
- **配置驱动设计**：把题目/维度/反向标记/解读文案外置为数据，让 AI 改动的影响面可控。
- **把验证装置作为一等公民**：独立重写的评分交叉验证、真实浏览器渲染冒烟、E2E 契约测试，都是为防止"盲信 AI 输出"而专门建设的。
- 完整过程见 **`docs/AI-DEVELOPMENT-RECORD.md`（交付物 C）**。

---

## 7. 从真实参与者身上学到了什么

> 完成证据与匿名数据附录见 **`docs/PILOT-EVALUATION.md`（交付物 D）**；以下为分析结论。
> 数据基底：管理端导出 **11 人开始、11 人完成（100% 完成率）**，满足"≥10 名独立参与者完成"。所有分数由 `scripts/analyze-pilot.mjs` 从 raw 独立重算，并与程序预计算逐格对账（最大差 0）。

按挑战赛 *Real-User Evaluation* 的 7 个维度逐条作答：

1. **用户能否成功完成** ✅：**11/11 完成（100%）**，耗时均值 213 秒（≈3.6 分钟，范围 73–455 秒），无卡顿或报错。说明问卷长度与节奏对本品类用户可接受。
2. **题目是否易懂** ⚠️（部分）：回收反馈称"测试内容较为专业规范"，但**长卷连续呈现**导致"做到后面已疲惫、容易漏看题目"——这是可读性 / 节奏问题，而非题目本身难懂。
3. **是否出现可用性问题** ⚠️：界面无阻塞性报错；但暴露几点体验短板——长卷疲劳与漏看风险、结果仅可分享链接而不能直接生成图片、以及**网址需挂 VPN 才能访问**（部署可达性）。
4. **评分系统是否如预期** ✅：**独立重算与预计算逐维度最大绝对差 = 0**，反向题重编码、维度均值、合成指数口径完全一致；反馈也提到"测试结果比较符合用户心理预期"，侧面印证结果面效度。
5. **是否存在异常应答模式** ⚠️：11 人中 **5 名**呈现明确低投入 / 机械作答信号（详见下表），另有 1 名边界案例——提示需加入作答质量监控。
6. **用户喜欢 / 不喜欢什么**：反馈要点已回收（见下方"用户反馈"小节），覆盖设计、专业性、选题、隐私、结果丰富度，以及上述可用性问题。
7. **下一版本会改进什么**：见 §7 末"改进点"。

### 7.1 完成耗时与异常模式

| 匿名 ID | 信号 | 说明 |
|---|---|---|
| `70d757b3` | 大五 O、C 维度直线 | 两个 8 题维度重编码后近乎同一值（sd≈0），疑似整段不加区分作答 |
| `c612851c` | 大五 E 维度直线 | 8 题外向性近乎同一值 |
| `bd31a38b` | 极速完成（73 秒） | ≈1.2 秒/题答完 60 题，远低于合理阅读速度 |
| `cb7a8ad7` | 周期 8 严格循环 | 全卷呈固定循环 `2,3,4,5,4,3,2,1` |
| `d3aa6f7f` | 严格交替 | 后半卷呈 `4,2,4,2…`（符号变化率 0.90） |
| `008b1a00`（边界） | PU、TR 两子量表全同 | 仅两个 4 题 AI 子量表一致，较轻，单列不计入主计数 |

> 5/11（45%）+ 1 边界表明：即使在"朋友/同学"这类善意样本中，**无提醒的匿名长卷仍会诱发模式化作答**。这不影响"≥10 人完成"达标，但确证了下版加入作答质量监控与注意力检测题的必要性。

### 7.2 维度描述性概览（探索性，n = 11）

均值 / 标准差（范围 1–5，量表中点 3.0），**仅作相对中点的启发式参考，无常模、无高/低诊断含义**。

| 构念 | 维度 | 均值 | 标准差 | 最小 | 最大 |
|---|---|---:|---:|---:|---:|
| 大五 | O 开放性 | 3.48 | 0.58 | 2.75 | 4.38 |
| 大五 | C 尽责性 | 3.43 | 0.37 | 3.00 | 4.13 |
| 大五 | E 外向性 | 2.92 | 0.59 | 2.00 | 4.00 |
| 大五 | A 宜人性 | 3.35 | 0.32 | 3.00 | 4.13 |
| 大五 | N 神经质 | 3.02 | 0.63 | 1.88 | 4.00 |
| AI 态度 | PU 感知有用性 | 3.77 | 0.88 | 2.00 | 5.00 |
| AI 态度 | TR 信任 | 3.55 | 0.53 | 2.75 | 4.25 |
| AI 态度 | WA 采纳意愿 | 3.82 | 0.86 | 2.00 | 4.75 |
| AI 态度 | LA 学习态度 | 3.66 | 0.54 | 3.00 | 4.75 |
| AI 态度 | CN 对 AI 的担忧 | 3.18 | 0.46 | 2.50 | 4.00 |
| AI 态度 | **合成指数** | **3.52** | **0.39** | 3.10 | 4.10 |

**试点观察（非结论）**：本样本对 AI 的态度整体偏积极（PU/WA 最高 ≈ 3.8，高于中点）；大五各维接近中点（E、N 略低于 3.0）；CN（担忧）约 3.2 处于中点附近——与"积极但非盲目乐观"的直觉相符。所有判断带小样本与样本同质性的强 caveat。

### 7.3 信度（Cronbach's α，探索性，n = 11，**不可用于推论**）

| 构念 | 维度 | α |
|---|---|---:|
| 大五 | O | 0.63 |
| 大五 | C | 0.18 |
| 大五 | E | 0.77 |
| 大五 | A | -0.21 |
| 大五 | N | 0.67 |
| AI 态度 | PU | 0.73 |
| AI 态度 | TR | 0.02 |
| AI 态度 | WA | 0.82 |
| AI 态度 | LA | 0.50 |
| AI 态度 | CN | 0.11 |

**解读（强 caveat）**：α 在 n = 11 下极不稳定；部分维度 α 近 0 或为负（C、A、TR、LA、CN），**最合理的解释是（a）样本极小，（b）§7.1 中模式化 / 满意化作答稀释了题项间协方差**，而非量表本身失效。这与本项目一贯的诚实立场一致：**试点用于发现问题，而非声称量表达到信度标准**。若需可信 α，须更大样本且先剔除模式化应答。

### 7.4 用户反馈（直接引述要点）

回收到的主观反馈（已获授权用于本交付物），按"喜欢 / 不喜欢"归类：

**喜欢的方面（优点）**
- 问卷**简约美观**；
- 测试内容**较为专业规范**；
- 测试结果**比较符合用户心理预期**（与 §7 第 4 点「评分系统是否如预期」相互印证）；
- 选题（**对 AI 的态度**）**新颖**；
- **不收集个人信息、补充信息可选，比较安全**；
- **指标完整、结果丰富、分析专业全面客观**。

**不喜欢的方面（缺点 / 改进诉求）**
- 题目**连续呈现较长**，做到后面已**疲惫、容易漏看题目** → 建议**点击选项后自动跳转**；
- 结果虽然可分享，但**不如直接生成图片**；
- **网址需要挂 VPN** 才能访问。

### 7.5 下一版本改进点（优先级）

1. **作答质量监控 + 注意力检测题**：结果 / 管理端加入 `detectResponseQuality` 实时提示，标记直线 / 周期 / 极速作答，并支持导出质量标签（代码层已预留 `qualityFlags`；本次试点证明它确有必要）；加入 1–2 道陷阱题。
2. **问卷节奏优化**：针对"长卷疲劳、漏看"——可考虑**分节 / 分页呈现**，并支持**点击选项后自动跳转下一题**（来自反馈）。
3. **结果导出为图片**：在"可分享链接"之外，提供一键生成结果卡片图片，满足用户直接分享的需求。
4. **部署可达性**：解决"需挂 VPN"问题（如更换/补充无需翻墙的访问方式或国内可访问的托管），降低参与者门槛。
5. **导出原子化**：管理端两类导出已在本轮基于同一最新快照生成；保持该一致性，避免跨时间导出集合不一致。
6. **更大、更异质的样本**：当前样本偏年轻 / 高学历 / 男性，需扩充以支撑任何推论性结论；人口学字段可改为显式"不愿透露"选项而非留空。

**分析立场（诚实性）**：本试点样本同质（18–25 岁、硕士为主、男性偏多）且 n = 11，探索性 Cronbach's α 在部分维度近 0 或为负——最合理的解释是小样本叠加模式化作答稀释了题项协方差，**不代表量表失效**，也**不构成任何推论性结论**。这与本项目"试点用于发现问题、而非声称验证"的一贯立场一致。

---

## 8. 遇到了什么问题

- **部署实操门槛**：Vercel 的 Production Branch 须设为 `master` 且环境变量齐备，否则构建失败或显示 "No Production Deployment"；Neon 连接串的直连 vs. Pooled 区别也曾导致"创建匿名参与者失败"。
- **Windows 环境下工具链差异**：`cmd` 不支持 POSIX 式环境变量赋值；本机缺 Node 使 `npm` 不可用。
- **跨平台构建坑**：G: 盘上 `fs.readlink` 误报 `EISDIR`，需 `readlink-polyfill.cjs` 兜住。
- **评分解释层的专业性**：初版把合成指数当成"得分"、分带用词易误导——经心理测量审查后纠正（见 §4、§9）。
- **数据污染**：测试期反复提交会污染聚合统计——已在管理端加"提交明细 + 删除"解决。

---

## 9. AI 犯过哪些错误

（详述见交付物 C §7）节选：

1. 在 Windows `cmd` 下给出无效的 `DATABASE_URL="..." command` 赋值语法。
2. 假设本机有 Node，导致 `npm` 命令不可用。
3. `vercel-build` 因 `readlink` 误报 `EISDIR` 而崩溃。
4. 结果页编辑缺失 `Button` 导入，致生产构建失败。
5. 一个单元测试对"两相同值"误判通过。
6. 评分改动遗留的类型回归（`qualityFlags` 未补 fixture）。
7. 知情同意措辞与"管理端可删除"功能自相矛盾。

---

## 10. 如何验证并纠正那些错误

- **类型检查（`tsc --noEmit`）** 在构建前拦下缺失 import 与类型回归。
- **`deploy:check` 门**（密钥扫描 → 生产自检 → 类型检查 → 测试）使损坏状态无法上线。
- **真实浏览器渲染冒烟** 抓出单元 + E2E 都漏掉的静默客户端渲染失败。
- **独立重写实现（`verify:scoring`）** 为评分数学提供外部校验。
- **静态守卫覆盖扫描** 保证每个 `/api/admin/*` 路由都受保护。
- **两轮只读专家审查** 在改代码前充当人工主导的验证。

---

## 11. 若再多一周，会改进什么

1. **（已完成）真实用户试点（Deliverable D）** 已收集 11 名完成者（完成率 100%）并独立分析，客观指标与主观反馈均已纳入 §7；下一步是扩充更异质样本、做更严谨效度证据（如 BFI-10 收敛效度、AI 态度 EFA）。
2. **补充逐维度分数直方图**，更严格满足"score distributions"要求。
3. **引入更严谨的效度证据**：如与大五短版量表（BFI-10）做收敛/区分效度对照，或对 AI 态度做探索性因子分析（EFA）检验"CN 是否确为独立因子"。
4. **管理后台增强**：提交明细支持按状态/维度筛选、批量删除，以及一键导出脱敏数据。
5. **隐私合规增强**：提供参与者"凭链接自行删除自己作答"的自助通道，而非仅管理员删除。
6. **国际化的 README 与文档**：当前仓库文档以中文为主，可为英文评审补充英文摘要页。

---

*本技术报告与 `docs/AI-DEVELOPMENT-RECORD.md`（交付物 C）、`docs/assessment-framework.md`（评分权威规格）互为补充。所有关于代码与数据的陈述均基于本工程真实实现，未经证实的结论（如试点结果）已明确标注为待完成。*
