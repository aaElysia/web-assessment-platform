# UX 改进落地记录（P0 → P1）

- **日期**：2026-09-12
- **依据**：`docs/ux-review-2026-09-12.md`
- **范围**：仅改体验层（Consent / Questionnaire / Result / 导航），**未触碰评分与统计计算口径**（与心理测量审查结论一致）。
- **验证**：`vitest run` 252 passed（15 文件）；`vercel-build`（typecheck + next build）exit 0；本地 Prisma Client 已切回 SQLite。

---

## 已实现

### P0（直接命中三大目标）

| 编号 | 改动 | 文件 |
|---|---|---|
| **P0-1** | 新增 `src/lib/site-config.ts`（反馈入口，零后端）。结果页与首页均增加「留下反馈」入口（邮件 / 第三方问卷，可一键替换）；结果页增加「复制结果链接」按钮（P1-7 一并落地）。 | `site-config.ts`、`result/page.tsx`、`page.tsx`、`Button` 复用 |
| **P0-2** | 同意页补充「数据存储与保留」条目：说明数据存于受管云数据库（与设备分离）、匿名随机 ID、无法单条删除、保留用于聚合分析、删除/导出政策联系渠道。 | `consent/page.tsx` |
| **P0-3** | 移动端 Likert 适配：题库增加 `shortLabel`；窄屏（<sm）显示「数字 + 缩写标签」，≥sm 显示完整文案；每个按钮 `min-h-[44px]`，满足移动端点击热区规范。 | `question-bank.json`、`loader.ts`、`api.ts`（透传 `shortLabel`）、`LikertScale.tsx`、`assessment/page.tsx` |

### P1（明显提升完成率 / 理解度）

| 编号 | 改动 | 文件 |
|---|---|---|
| **P1-4** | 作答页「保存并稍后继续」CTA：显式持久化草稿、平滑回顶、显示「已保存 ✓」反馈。 | `assessment/page.tsx` |
| **P1-5** | 雷达图「未计分」维度图例改为空心环标记，并在图下追加固定说明：「落点仅为占位示意，不代表水平极低」。 | `BigFiveRadar.tsx` |
| **P1-6** | 结果页增加「你的倾向小结（仅供参考）」卡片，由纯函数 `buildSummary` 生成，仅用分带标签（高于/接近/低于中点）+ 维度名，**绝不出现诊断性定性措辞**。 | `view-model.ts`、`result/page.tsx`、`view-model.test.ts`（新增 4 条断言） |
| **P1-7** | 见 P0-1（复制结果链接按钮）。 | `result/page.tsx` |

### P2（锦上添花，本次顺带完成）

- **P2-8** 作答页滚动超一屏后右下角出现「↑ 回到顶部」悬浮按钮。
- **P2-10** 吸顶进度条在「全部作答完」时显示「去提交 ↓」链接，平滑滚动到底部提交区。

---

## 未做（P2-admin 每日趋势）

管理端「每日完成趋势图」属增量项，工作量中等、且本次目标聚焦前端体验，暂未实现；如需要可单独排期（数据源已在 `admin/stats` 具备日粒度统计）。

---

## 部署提醒

- 本次改动已通过构建，但**尚未推送到 GitHub**：`git push` 后 Vercel 会自动重新部署（前提：Production Branch = `master`）。
- 反馈入口默认 `mailto:web-assessment-feedback@example.com`，上线前请替换为你的真实邮箱或第三方问卷链接（改 `src/lib/site-config.ts` 一处即可）。
- 上次建表用过的真实 Neon 直连串，请确认已在 Neon 重置密码，并把 Vercel 的 `DATABASE_URL` 同步换为新串。
